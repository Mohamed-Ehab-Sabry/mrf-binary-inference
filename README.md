# mrf-binary-inference
A pure Python, from-scratch implementation of a Markov Random Field (MRF) forming a triangle network with three binary variables. This repository includes exact inference, partition function calculation, and marginalization without the use of external probability libraries.
